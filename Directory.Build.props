<Project>
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Only fail on high/critical vulnerabilities; medium stays visible in warnings -->
    <NuGetAuditLevel>high</NuGetAuditLevel>
  </PropertyGroup>
</Project>
